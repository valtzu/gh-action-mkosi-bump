[Distribution]
Distribution=debian
Release=testing
Snapshot=20240101T000000Z

[Build]
# ToolsTree=default is deliberately set: `mkosi latest-snapshot` must still work
# (the action passes `--tools-tree=` so it uses the host curl, not the unbuilt tree).
ToolsTree=default

[Output]
Format=directory
